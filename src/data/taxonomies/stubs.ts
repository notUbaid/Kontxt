import type { Category } from './types';

// This is a placeholder for the 12 additional app types.
// The user will provide the exact details for each of these in the future.
// Each should eventually be extracted into its own file (e.g. mobileApp.ts) to prevent spillover.

export const emptyTaxonomy: Category[] = [
  {
    id: 'placeholder',
    name: 'COMING SOON',
    topics: []
  }
];
